{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile"
  },
  "deploy": {
    "startCommand": "pnpm runtime:refresh",
    "cronSchedule": "*/30 * * * *",
    "restartPolicyType": "NEVER"
  }
}
